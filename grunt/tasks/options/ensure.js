module.exports = {
  dev: {
    watch: '<%= app_files.ensure.watch %>',
    match: '<%= app_files.ensure.match %>',
    cwd: '<%= app_files.ensure.cwd %>',
    ext: '<%= app_files.ensure.ext %>'
  }
};